fastapi>=0.115.0
uvicorn[standard]>=0.30.6
sqlalchemy>=2.0.35
alembic>=1.13.2
psycopg2-binary>=2.9.12
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-jose[cryptography]>=3.3.0
cryptography>=50.0.0   # pin above python-jose's floor to clear PYSEC-2026-3552
bcrypt>=4.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
email-validator>=2.2.0
httpx>=0.27.2          # Resend / Brevo email API calls (app.services.notifications)
boto3>=1.34.0          # S3-compatible attachment storage (Cloudflare R2); only used when STORAGE_BACKEND=s3
slowapi>=0.1.9         # per-IP rate limiting on the login endpoint
sentry-sdk[fastapi]>=2.0.0  # error tracking; no-op unless SENTRY_DSN is set
openpyxl>=3.1.0        # M8 reports — Excel export
fpdf2>=2.7.0          # M8 reports — PDF export (pure Python, no system deps)

# dev/test only
pytest>=8.3.3
ruff>=0.16.0
pip-audit>=2.7.0
