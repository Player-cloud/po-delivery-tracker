# Loaded automatically by `next build` (production mode) and by
# scripts/gen-headers.mjs. The API origin is public — it ships in the client
# bundle — so it lives in the repo, not just the Cloudflare build env.
# Override locally with .env.local if you build against a different backend.
NEXT_PUBLIC_API_BASE_URL=https://po-tracker-api-q3b3.onrender.com
