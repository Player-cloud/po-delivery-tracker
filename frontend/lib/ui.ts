// Shared class strings so every screen looks the same.

export const page = "mx-auto max-w-6xl px-4 py-6 sm:px-6";

export const card =
  "rounded-[var(--radius-card)] border border-line bg-surface shadow-card";

export const btnPrimary =
  "inline-flex items-center justify-center gap-1.5 rounded-[var(--radius-control)] bg-accent px-4 py-2 text-sm font-semibold text-accent-on transition-colors hover:bg-accent-hover disabled:opacity-50";

export const btnGhost =
  "inline-flex items-center justify-center gap-1.5 rounded-[var(--radius-control)] border border-line bg-surface px-4 py-2 text-sm font-medium text-ink transition-colors hover:bg-page disabled:opacity-50";

export const input =
  "rounded-[var(--radius-control)] border border-line bg-surface px-3 py-2 text-sm placeholder:text-faint focus:border-accent focus:outline-none";

export const h1 = "font-display text-[22px] font-semibold tracking-tight";
